# Project screenshots

Put images here so they are served from the site root, e.g.:

- File: `public/projects/okr-validator/dashboard.png`
- Frontmatter path: `/projects/okr-validator/dashboard.png`

You can also use **full URLs** to images hosted elsewhere (CDN, Imgur, etc.) in the `screenshots` list.
